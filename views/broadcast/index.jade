block content
  p Welcome to list of broadcasts
  ul
    each rec in data
      li
        <a href=/broadcasts/#{rec._id}>#{rec.url}</a>

        👁#{rec.join}
        📜#{rec.comment}
        💖#{rec.heart}
